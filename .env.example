# Database
POSTGRES_DB=mes
POSTGRES_USER=mes
POSTGRES_PASSWORD=
DATABASE_URL=postgresql://mes:WACHTWOORD@postgres:5432/mes

# Redis
REDIS_URL=redis://redis:6379

# Security
JWT_SECRET=                   # min 32 tekens — genereer met: openssl rand -hex 32
BC_ENCRYPTION_KEY=            # exact 64 hex tekens (32 bytes) — genereer met: openssl rand -hex 32

# Business Central (kan ook via admin UI worden ingevuld)
BC_TENANT_ID=
BC_CLIENT_ID=
BC_CLIENT_SECRET=
BC_BASE_URL=https://api.businesscentral.dynamics.com/v2.0/TENANT/ENVIRONMENT/api/v2.0
